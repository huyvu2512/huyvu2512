<!-- 💚 Mint Glow Animated Banner by Huy Vũ -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 400" width="900" height="400">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        /* Hiệu ứng chuyển màu nền */
        @keyframes gradientShift {
          0% { background-position: 0% 50%; }
          50% { background-position: 100% 50%; }
          100% { background-position: 0% 50%; }
        }

        /* Hiệu ứng nổi */
        @keyframes float {
          0%, 100% { transform: translateY(0); }
          50% { transform: translateY(-10px); }
        }

        /* Hiệu ứng phát sáng xanh lá nhạt */
        @keyframes glow {
          0%, 100% {
            text-shadow:
              0 0 10px #caffb5,
              0 0 20px #a8ff94,
              0 0 40px #7fff6e,
              0 0 60px #5cff54;
          }
          50% {
            text-shadow:
              0 0 15px #e6ffe6,
              0 0 30px #b8ff8a,
              0 0 50px #96ff70,
              0 0 70px #66ff4f;
          }
        }

        /* Vùng chứa */
        .container {
          font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
          display: flex;
          align-items: center;
          justify-content: center;
          height: 400px;
          background: linear-gradient(270deg, #003d1a, #058c3f, #18c76b, #7effa4);
          background-size: 600% 600%;
          animation: gradientShift 12s ease infinite;
          border-radius: 20px;
          box-shadow: 0 0 40px rgba(130, 255, 150, 0.25);
        }

        /* Tiêu đề chính */
        .title {
          font-size: 64px;
          font-weight: 800;
          color: #eaffea;
          animation:
            float 3s ease-in-out infinite,
            glow 2.5s ease-in-out infinite alternate;
          text-align: center;
          line-height: 1.2;
          letter-spacing: 4px;
          text-transform: uppercase;
        }

        /* Dòng phụ */
        .subtitle {
          font-size: 22px;
          color: rgba(240,255,240,0.9);
          margin-top: 12px;
          animation: float 3s ease-in-out infinite reverse;
        }
      </style>

      <div class="container">
        <div>
          <div class="title">💚 Huy Vũ 🐷 💚</div>
          <div class="subtitle">Creative Developer • Code with Passion 🌱</div>
        </div>
      </div>
    </div>
  </foreignObject>
</svg>
